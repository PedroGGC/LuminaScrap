# Plan 01-01: Monorepo Scaffold + Drizzle Schema + Neon Setup

**Phase:** 1 (Foundation & MVP)
**Status:** Pending execution

## Objective

Scaffold the project structure, create Drizzle schema for Neon Postgres, set up migrations, and configure the development environment.

## Requirements Covered

- DATA-01: products table with id, nome, url, site, categoria, created_at
- DATA-02: notebooks table extending products with cpu, gpu, ram, storage_type, storage_size, tela, marca
- DATA-03: prices table append-only (product_id, valor, scraped_at)
- DATA-04: product_groups table (empty for future dedup)
- DATA-05: Migrations versioned via Drizzle

## Tasks

### 1.1 Initialize Project Structure
```
[TASK] Create TypeScript monorepo structure
  - Initialize package.json with pnpm workspaces
  - Set up shared types package
  - Create API package (Fastify)
  - Create scraper package
  - Create frontend (Next.js) in separate repo or workspace
```

### 1.2 Database Schema
```
[TASK] Create Drizzle schema in api/src/db/schema.ts
  - products table
  - notebooks table
  - prices table
  - product_groups table (empty)
  - scrape_logs table
  - parse_errors table
```

### 1.3 Neon Setup
```
[TASK] Configure Neon database connection
  - Create Neon project (free tier)
  - Configure connection string in .env
  - Test connection
```

### 1.4 Migrations
```
[TASK] Set up Drizzle migrations
  - Install drizzle-kit
  - Create first migration for schema
  - Test migration against Neon
```

### 1.5 Development Scripts
```
[TASK] Add dev scripts to package.json
  - db:push (schema to DB)
  - db:migrate (versioned migrations)
  - db:studio (DB browser)
```

## Threat Model

- **Data exposure:** Neon connection string in .env — ensure .gitignore excludes .env
- **Schema migration:** Automated migrations could cause downtime — use caution in production

## Dependencies

- `drizzle-orm` + `drizzle-kit`
- `@neondatabase/serverless`
- TypeScript workspaces

## Success Criteria

- [ ] Monorepo structure created with pnpm workspaces
- [ ] Schema applied to Neon with all 6 tables
- [ ] .env configured and .gitignored
- [ ] Migrations can be run via CLI

## Files to Create/Modify

- `api/package.json` — workspace member
- `api/src/db/schema.ts` — Drizzle schema
- `api/src/db/index.ts` — DB connection
- `drizzle.config.ts` — migration config
- `.env` — Neon connection (add to .gitignore)
- `.gitignore` — exclude .env
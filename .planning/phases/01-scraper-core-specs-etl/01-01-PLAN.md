---
phase: 1
plan: 01-01-PLAN
type: standard
wave: 1
depends_on: []
files_modified:
  - package.json
  - scraper/package.json
  - scraper/tsconfig.json
  - scraper/src/index.ts
  - scraper/src/extractors/base.ts
  - scraper/src/extractors/pichau.ts
  - scraper/src/extractors/terabyte.ts
  - scraper/src/schemas/hardware.ts
  - scraper/src/utils/delay.ts
autonomous: true
requirements:
  - Scraper CLI
  - CPU/GPU Zod Schemas
  - Consolidated JSON compilation
---

<objective>
Initialize the Bun project structure for the web scraper, define Zod schemas for CPU/GPU data normalization, implement Pichau and Terabyte crawlers using Playwright and Cheerio, and compile results into a consolidated JSON file.
</objective>

<tasks>
  <task id="1" wave="1">
    <read_first>
      - .planning/phases/01-scraper-core-specs-etl/01-CONTEXT.md
      - .planning/phases/01-scraper-core-specs-etl/01-RESEARCH.md
    </read_first>
    <action>
      Initialize Bun workspace in the `scraper/` directory. Create `scraper/package.json` and configure `dependencies` to install: `playwright`, `cheerio`, `zod`, `commander`. Create `scraper/tsconfig.json` with strict type checking. This satisfies D-01.
    </action>
    <verify>
      Verify that `scraper/package.json` exists and runs `bun install`.
    </verify>
    <acceptance_criteria>
      - `scraper/package.json` exists and includes playwright, cheerio, zod, and commander dependencies.
      - `scraper/tsconfig.json` exists with strict compilation rules.
    </acceptance_criteria>
  </task>

  <task id="2" wave="1">
    <read_first>
      - .planning/phases/01-scraper-core-specs-etl/01-RESEARCH.md
    </read_first>
    <action>
      Implement Zod schemas in `scraper/src/schemas/hardware.ts`. Create `BaseProductSchema`, `CpuProductSchema`, and `GpuProductSchema`. The schemas must clean price strings like "R$ 1.234,56" into float values and normalize socket/VRAM values (e.g. converting sockets to AM4/AM5/LGA1700 and checking VRAM formats). This satisfies D-04, D-05, and D-06.
    </action>
    <verify>
      Create a test script or run a module import verification to ensure Zod successfully validates and parses raw data into normalized specifications.
    </verify>
    <acceptance_criteria>
      - `scraper/src/schemas/hardware.ts` exports `CpuProductSchema` and `GpuProductSchema`.
      - Zod schemas parse and transform raw pricing strings (e.g. "R$ 1.500,00" to 1500) and normalize sockets (e.g. "lga 1700" to "LGA1700", "am4" to "AM4").
    </acceptance_criteria>
  </task>

  <task id="3" wave="1">
    <read_first>
      - scraper/src/schemas/hardware.ts
    </read_first>
    <action>
      Create `scraper/src/extractors/base.ts` defining the common `BaseExtractor` class and `Extractor` interface. Create a delay utility in `scraper/src/utils/delay.ts` exposing a random sleep helper. This satisfies D-02.
    </action>
    <verify>
      Ensure the base extractor exports compile with no errors.
    </verify>
    <acceptance_criteria>
      - `scraper/src/extractors/base.ts` exists and exposes interface `Extractor` with method `scrape()`.
      - `scraper/src/utils/delay.ts` exports a working wait/sleep utility.
    </acceptance_criteria>
  </task>

  <task id="4" wave="1">
    <read_first>
      - scraper/src/extractors/base.ts
      - scraper/src/schemas/hardware.ts
    </read_first>
    <action>
      Implement `PichauExtractor` in `scraper/src/extractors/pichau.ts`. Use Playwright to load dynamic pages (CPUs/GPUs lists), extract title, price, and specs using query selectors, parse with Cheerio if beneficial, and return raw product lists. This satisfies D-02 and D-03.
    </action>
    <verify>
      Run extractor individually via a scratch script.
    </verify>
    <acceptance_criteria>
      - `scraper/src/extractors/pichau.ts` compiles and correctly launches chromium context.
      - Playwright navigates product listings and extracts un-normalized product objects.
    </acceptance_criteria>
  </task>

  <task id="5" wave="1">
    <read_first>
      - scraper/src/extractors/base.ts
      - scraper/src/schemas/hardware.ts
    </read_first>
    <action>
      Implement `TerabyteExtractor` in `scraper/src/extractors/terabyte.ts`. Use Playwright / Cheerio to crawl listings, fetch dynamic content, and parse lists. This satisfies D-02 and D-03.
    </action>
    <verify>
      Run extractor individually.
    </verify>
    <acceptance_criteria>
      - `scraper/src/extractors/terabyte.ts` compiles and correctly launches browser context.
      - Product selectors successfully extract pricing and specifications.
    </acceptance_criteria>
  </task>

  <task id="6" wave="1">
    <read_first>
      - scraper/src/extractors/pichau.ts
      - scraper/src/extractors/terabyte.ts
      - scraper/src/schemas/hardware.ts
    </read_first>
    <action>
      Create `scraper/src/index.ts` as the CLI runner. Implement options using commander for `--retailer` (pichau/terabyte/all) and `--type` (cpu/gpu/all). Fetch raw data, feed it through the Zod schemas for ETL, consolidate the successfully validated products, and write them as a single JSON file to `produtos.json`. This satisfies D-07.
    </action>
    <verify>
      Run `bun run src/index.ts --retailer all --type all` and verify `produtos.json` outputs successfully.
    </verify>
    <acceptance_criteria>
      - CLI entry point processes arguments using Commander.
      - Runs scraper pipelines, filters outliers/invalid data using Zod, and creates `produtos.json` with a list of valid items.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
We will verify the implementation by:
1. Running `bun install` to ensure all packages download properly.
2. Implementing unit tests or simple runner validation using Bun's test runner to confirm that raw pricing strings and hardware socket details are normalized.
3. Executing the CLI command `bun run src/index.ts --retailer all --type all` to check that the scrapers crawl Pichau and Terabyte and compile results into `produtos.json` without failing.
</verification>

<success_criteria>
- `bun run src/index.ts` completes successfully with no runtime errors.
- A valid, normalized `produtos.json` file is produced containing pricing, name, link, retailer source, and specifications for CPUs and GPUs.
- All product records conform strictly to `CpuProductSchema` and `GpuProductSchema` definitions.
</success_criteria>

### must_haves
#### truths
- D-01: Scraper runs locally on Bun runtime using TypeScript.
- D-02: Modular design where each retailer has its own class/module conforming to a common extractor interface.
- D-03: Playwright and Cheerio are used for extraction.
- D-04: Zod schemas clean, validate, and normalize product data (CPUs and GPUs).
- D-05: Price strings are parsed into clean float numbers.
- D-06: Technical specifications are normalized.
- D-07: Output is a consolidated `produtos.json` file.

#### prohibitions
- Must not write unvalidated data (e.g. products missing price or mandatory specifications) to the final JSON.

## Artifacts this phase produces
- `scraper/package.json`
- `scraper/tsconfig.json`
- `scraper/src/index.ts`
- `scraper/src/schemas/hardware.ts`
- `scraper/src/extractors/base.ts`
- `scraper/src/extractors/pichau.ts`
- `scraper/src/extractors/terabyte.ts`
- `scraper/src/utils/delay.ts`
- `produtos.json`

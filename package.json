{
  "name": "notebook-price-painel",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "scrapers"
  ],
  "scripts": {
    "dev": "bun --parallel --filter api --filter scrapers run dev",
    "build": "bun -r run build",
    "db:push": "bun --filter api run db:push"
  },
  "engines": {
    "bun": ">=1.0"
  },
  "packageManager": "bun@1.0.0",
  "devDependencies": {
    "@types/node": "^25.7.0",
    "drizzle-kit": "^0.31.10"
  }
}